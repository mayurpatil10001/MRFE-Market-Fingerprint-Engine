[pytest]
addopts = -ra -q --strict-markers --strict-config --cov=src/domain --cov=src/application --cov-report=term-missing --cov-fail-under=90
testpaths = tests
asyncio_mode = auto
markers =
    integration: integration tests
    e2e: end-to-end tests
    performance: performance and load test hooks
